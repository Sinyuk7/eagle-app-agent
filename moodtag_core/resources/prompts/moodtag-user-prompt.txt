Analyze this moodboard image and output ONLY valid JSON using this exact template and key order:

{{json_template}}

Field definitions:
- brief: One concise Chinese subject-verb-object sentence describing visible image content. Start with the main subject, then action/pose/state, then scene or key props when visible. It answers "图里是什么". Do not write abstract evaluation such as "视觉冲击力", "独特氛围", "充满动感", or "核心价值".
- elements: Array of 5-10 visible, relatively static, searchable Chinese noun phrases. Include only character appearance, clothing, props, scene objects, and concrete visual symbols. Do NOT include action/state, camera/framing, composition, lighting, mood, style labels, or use-intent words.
- use: One natural Chinese sentence explaining how this image should be used later. Prefer "主要用于……参考，也可作为……参考。"
- key: Chinese natural language describing the strongest visual hook and why the image is worth saving.
- camera: Chinese natural language with key professional terms in English. Describe shot size, angle, lens feel, perspective, framing, subject distance, and scale when visible.
- light_color: Chinese natural language with key professional terms in English. Describe lighting and color as one creative system.
- tags: Array of exact tags selected only from the allowed taxonomy.
- use_intents: Array of exact internal use intent ids selected only from the left side of the allowed use intents list.

Language rules:
- Field names stay English through the JSON keys.
- Natural-language field values should be Chinese-first.
- Keep professional visual terms in English when useful, such as wide angle perspective, telephoto compression, natural diffused light, color accent, low angle, centered.
- Do not include tags, suggested tags, or use intent ids in brief/elements/use/key/camera/light_color.
- Elements should look like "白发少女", "角饰", "黑色战术服", "蓝色弹药带", "户外街道", "废墟拱门"; never like "风吹发丝", "低机位", "动态姿势", "戏剧性", "pose ref".
- Do not invent tags.
- If a tag is uncertain, omit it.
- Do not output pipeline tag names inside use_intents. For example, output "pose_reference", not "pose ref"; output "aigc_prompt_reference", not "prompt ref".
- Do not repeat any tag or use_intent.

Allowed taxonomy:
{{taxonomy}}

Allowed use intents:
{{use_intents}}

Output rules:
- Output MUST be raw JSON only.
- Do NOT wrap in markdown or code blocks.
- Response must start with { and end with }.
- No text before or after JSON.
- Close every array and close the final JSON object.
